[phases.setup]
nixPkgs = ["libreoffice", "poppler_utils", "ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 -m gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
