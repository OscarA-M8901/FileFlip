[phases.setup]
nixPkgs = ["libreoffice", "poppler_utils", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
